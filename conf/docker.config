/*
 * -------------------------------------------------
 * Nextflow config file for ICGC-featureCounts
 * -------------------------------------------------
 * Docker images for every process
 * All images will be pulled automatically
 * -------------------------------------------------
 */

docker {
  enabled = true
}
